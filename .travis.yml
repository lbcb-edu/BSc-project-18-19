language: cpp

compiler:
  - clang
  - gcc
  
os:
  - ubuntu
  - macOS

script:
    - mkdir build
    - cd build
    - cmake -DCMAKE_BUILD_TYPE=Release -Dblue_mapper_build_tests=ON ..
    - make
    - ./blue_mapper_tests
    
